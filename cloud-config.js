/* ============================================================
   云服务公开配置（cloud-config.js）
   ------------------------------------------------------------
   这两个值来自"开通云服务"时返回的 publicConfig。
   ⚠️ 关键知识点（AGENTS.md 五.3：密钥不进代码）——
     publishableKey 不是密码！它只用来"表明你是哪个应用"，
     本身不带任何权限；服务端还会校验请求来自哪个网址，
     网址对不上就拒绝。所以它可以放在前端代码里。
     真正的密钥在服务器那边，我们从头到尾都看不到，也不需要。

   这个文件单独拆出来，是为了"配置和业务代码分开"：
   哪天云端信息变了，只改这一个文件，三个页面都跟着变。
   ============================================================ */

var cloudConfig = {
  // 云端数据服务的总入口地址
  endpoint: 'https://trpg-card-gallery.app.workbuddy.host',
  // 应用的公开标识（可公开，非密钥）
  publishableKey: 'wbpk_9aEc5o9uyUYcJR6Xy4jU7x_SAMq8gEVsm2wDWSqTE6aJ4GjEmUxGIsi',
};
